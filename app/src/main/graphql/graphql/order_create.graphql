mutation order_create($input: CreateOrderInput!) {
    order_create(input: $input) {
        id
        orderNumber
        quantity
        status
        total {
            amount
            currency
        }
    }
}